# Used only during `docker build` so `prisma generate` can read prisma.config.ts.
# No database connection happens at build time. Runtime URL comes from docker-compose.
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/dummy?schema=public
